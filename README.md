# Time Series Analysis Project
Time series analysis using Machine Learning and Deep Learning in Python
## Setup
Create environment:

```bash
conda create -n ATD_03 python=3.10
conda activate ATD_03
```
Install dependencies:

```python
%pip install -r requirements.txt
```
## GPU -- 4060
Install PyTorch with CUDA support:

```bash
conda install -c pytorch -c nvidia pytorch torchvision torchaudio pytorch-cuda=12.1
```
